# Allow Finn's low-latency audio applications, including Ardour, to lock the
# memory they need into RAM.  This is a permission, not a reservation of RAM.
#
# Install as /etc/security/limits.d/99-ardour-memlock.conf, then begin a new
# login session for the limit to take effect.
finn    -    memlock    unlimited

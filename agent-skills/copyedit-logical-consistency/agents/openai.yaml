interface:
  display_name: "Copyedit (Logical consistency)"
  short_description: "NZ English logical consistency checks with automatic false-positive filtering"
  default_prompt: "Run $copyedit-logical-consistency on the provided text or file(s) and write copyedit_logical_consistency_feedback.md at the repo root."

policy:
  allow_implicit_invocation: true
